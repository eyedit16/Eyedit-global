<!doctype html>
<html lang="ca">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>EyeDit Group</title>
<style>
  body{font-family:system-ui;margin:0;padding:20px}
  nav{display:flex;gap:10px;flex-wrap:wrap;position:sticky;top:0;background:#fff;padding:10px 0}
  button{padding:10px 12px;border:1px solid #000;border-radius:10px;background:#fff}
  button.active{background:#000;color:#fff}
  .hidden{display:none !important}
  section{margin-top:18px;padding:14px;border:1px solid #000;border-radius:12px}
</style>
</head>
<body>

<nav>
  <button id="nav-home" class="active">Home</button>
  <button id="nav-audiovisual">Audiovisual</button>
  <button id="nav-ccs">CCS</button>
  <button id="nav-eyevision">EyeVision</button>
</nav>

<section id="page-home">
  <h1>Home</h1>
  <p>Si això funciona, el problema era el teu codi anterior (IDs/script).</p>
</section>

<section id="page-audiovisual" class="hidden">
  <h1>Audiovisual</h1>
  <p>OK</p>
</section>

<section id="page-ccs" class="hidden">
  <h1>CCS</h1>
  <p>OK</p>
</section>

<section id="page-eyevision" class="hidden">
  <h1>EyeVision</h1>
  <p>OK</p>
</section>

<script>
(() => {
  const pages = ['home','audiovisual','ccs','eyevision'];

  function showPage(key){
    pages.forEach(k=>{
      const el = document.getElementById('page-'+k);
      if(el) el.classList.toggle('hidden', k!==key);
    });
    pages.forEach(k=>{
      const btn = document.getElementById('nav-'+k);
      if(btn) btn.classList.toggle('active', k===key);
    });
    history.replaceState(null,'', key==='home' ? '#home' : '#'+key);
    window.scrollTo({top:0,behavior:'smooth'});
  }
  window.showPage = showPage;

  pages.forEach(k=>{
    const btn = document.getElementById('nav-'+k);
    if(btn) btn.addEventListener('click', () => showPage(k));
  });

  const start = (location.hash || '#home').replace('#','');
  showPage(pages.includes(start) ? start : 'home');
})();
</script>

</body>
</html>

